---
name: plan
description: Start or resume interactive feature planning. Maps how the existing code works first (end-to-end flow, grounded in file:line), then writes a plain-language IDEA.md north star (confirmed by the user), then drives a persistent docs/plans/<slug>/PLAN.md — full question queue lives in the doc, discussion happens one question at a time, each question tied back to the idea. Args - an existing slug to resume, or a short description to start a new plan. For a plan document written outside this workflow, use /adopt first.
---

Read `~/src/wheelchair/protocol/planning.md` and
follow it exactly. The skill argument is the input: an existing slug (resume) or a short
description (new plan).
