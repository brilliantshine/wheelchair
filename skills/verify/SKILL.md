---
name: verify
description: Blind verification of a completed implementation (docs/plans/<slug>/ with status verifying and COMPLETION.md present). A fresh verifier, cross-family when available, falsifies the completion claims against the plan spec; loops remediation until PASS. Args - the plan slug.
---

Read `{{WHEELCHAIR_ROOT}}/protocol/verification.md`
and follow it exactly. The skill argument is the plan slug.
