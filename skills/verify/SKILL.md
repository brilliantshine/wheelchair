---
name: verify
description: Blind cross-family verification of a completed implementation (docs/plans/<slug>/ with status verifying and COMPLETION.md present). A verifier from the opposite model family falsifies the completion claims against the plan spec; loops remediation until PASS. Args - the plan slug.
---

Read `{{WHEELCHAIR_ROOT}}/protocol/verification.md`
and follow it exactly. The skill argument is the plan slug.
