---
name: diagram-sensitivity
description: Report or move the dial controlling how readily an agent answers with a picture instead of prose alone - ask, default, or high. Use when asked what the current diagram sensitivity is, or to turn it up or down, or when asked for more or fewer diagrams by default. Args - nothing to report the current level, or one of ask, default, high to set it.
---

Read `{{WHEELCHAIR_ROOT}}/protocol/sensitivity.md` and follow it exactly. The skill
argument is the level to set — `ask`, `default` or `high` — or nothing, to report the
level currently set.
